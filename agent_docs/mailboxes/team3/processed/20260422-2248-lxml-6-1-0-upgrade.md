---
from: lead
to: 3
subject: lxml-6-1-0-upgrade
created: 2026-04-22T13:48:49Z
priority: Ask
---

# lxml 5.2.2 → 6.1.0 업그레이드 (CVE-2026-41066 / GHSA-vfmq-68hx-4jfw)

## 요약

공급망 보안 취약점 차단용 lxml 업그레이드. `backend/.pip-audit-ignore` + `.grype.yaml` 동시 ignore 엔트리 만료 전에 실제 버전 bump 로 전환. 리드 사전 진단: 앱 코드의 lxml 취약 sink 0건, 영향 범위 최소.

## 맥락

- 현재 ignore 엔트리 (동일 GHSA, 만료 2026-06-06):
  - `backend/.pip-audit-ignore:40` → `GHSA-vfmq-68hx-4jfw`
  - `.grype.yaml` 동일 GHSA 블록
- CLAUDE.md §9 의 두 항목이 본 건:
  - "lxml 6.1.0 업그레이드 (CVE-2026-41066 후속)"
  - 참고: "`fix/grype-yaml-glibc-lxml-parity`" 회고에서 pip-audit ↔ grype parity 병기 원칙 확립됨. 본 PR 에서도 **동일 커밋에서** 두 파일 수정 필수.
- 소유: `backend/core/data_collector/*` 가 팀 3 으로 확장된 2026-04-22 governance 갱신의 첫 실전 smoke test 대상.

## 사전 진단 (리드, 2026-04-22)

1. **lxml 취약 sink 직접 호출 0건**:
   ```
   grep -rnE 'iterparse|ETCompatXMLParser|etree\.parse\(|etree\.fromstring' backend/{core,api,scripts,alembic}
   ```
   결과: `backend/.pip-audit-ignore` 내의 *주석* 2건만 매칭. 앱 코드 0건.
2. **유일한 간접 사용** — `backend/core/data_collector/news_collector.py:222/224`:
   ```python
   content = BeautifulSoup(raw, "lxml").get_text(strip=True)
   content = BeautifulSoup(entry.summary, "lxml").get_text(strip=True)
   ```
   BeautifulSoup 의 `"lxml"` parser 는 HTML 경로로 XXE sink 영역 외.
3. **테스트 하니스 존재**: `backend/tests/test_news_collector.py` (+`test_scheduler_handlers.py:60` 에 간접 참조).

## 요청 / 정보 — 실행 체크리스트

### 1. 브랜치 생성

```bash
cd /Users/ahnsucheol/Desktop/aqts-team3-api   # 팀 3 worktree
git checkout main && git pull
git checkout -b team3/chore/lxml-6-1-0-upgrade
```

### 2. 파일 수정 (3 파일, 동일 커밋)

- **`backend/requirements.txt:71`**: `lxml==5.2.2` → `lxml==6.1.0`
- **`backend/.pip-audit-ignore:40`** 블록 전체 삭제 (설명 주석 라인 32~39 + GHSA 엔트리 40). 주변 다른 엔트리 건드리지 않기.
- **`.grype.yaml`** 의 `GHSA-vfmq-68hx-4jfw` 블록 전체 삭제 (설명 주석 포함). 다른 엔트리 (`GHSA-wj6h-64fc-37mp` glibc 등) 유지.

### 3. 로컬 재설치 + smoke test

```bash
pip install -r backend/requirements.txt --upgrade
python -c "import lxml; print(lxml.__version__)"   # 6.1.0 확인
python -c "from bs4 import BeautifulSoup; print(BeautifulSoup('<p>x</p>', 'lxml').get_text())"   # 'x' 출력

cd backend
python -m pytest tests/test_news_collector.py -v --tb=short
python -m pytest tests/test_scheduler_handlers.py::TestPreMarketHandler::test_pre_market_calls_news_collector -v
```

**실 RSS feed 수동 검증 (최소 2건)** — `news_collector._parse_entry` 가 실제 HTML 을 정상 파싱하는지. `backend/core/data_collector/news_collector.py` 상단의 DEFAULT_FEEDS (또는 동등 상수) 를 참조하여 1~2개 feed 를 실제 pull 후 `get_text()` 산출물을 lxml 5.x 버전과 비교. 문제 없으면 로그만 남기면 됨.

### 4. 전체 게이트 (커밋 전)

`backend/requirements.txt` 변경은 문서-only 예외 **불가**. 전체 게이트 필수:

```bash
cd backend
python -m ruff check . --config pyproject.toml
python -m black --check . --config pyproject.toml   # black==24.4.2 pinned
python -m pytest tests/ -q --tb=short               # timeout ≥ 540s
```

### 5. OPS 런북 작성

`docs/operations/lxml-6-1-0-upgrade-2026-04-22.md` (OPS-021) 신설:

- Before/After: `lxml==5.2.2` → `lxml==6.1.0`, ignore 2 파일 동시 제거
- 사전 진단 결과 (취약 sink 0건) 인용
- smoke test 로그 (pytest 출력 + 실 RSS pull 결과)
- 회귀 방어선: 향후 누군가 `iterparse` 또는 `ETCompatXMLParser` 를 새로 도입하면 별도 PR 로 ignore 재추가 필요성 검토 가이드

### 6. CLAUDE.md §9 TODO 전환 (리드 수행 — 본 PR 머지 후)

- `[ ] lxml 6.1.0 업그레이드` 를 `[x]` 로 전환 (리드 전용 파일)
- 본 PR 링크 + 머지 SHA 인용

## 응답 기한

**2026-04-29 (수) 23:59 KST** — W1 관찰 창 내 완료 목표.

만료일 하드 리밋은 2026-06-06 이지만, ADR-002 Stage 2 관찰과 동시 진행 시 팀 3 의 다른 급한 이슈가 끼어들 위험이 있어 마진 확보용 W1 내 완료. W2 (04-29 ~ 05-05) 연장 허용. 2026-05-23 까지 반드시 머지.

## 위험 / Stop 조건

- lxml 6.x 업그레이드 후 `pytest tests/test_news_collector.py` 실패 → 즉시 rollback + 본 메일박스로 역요청 (리드 판단 필요)
- `BeautifulSoup(raw, "lxml")` 호출이 lxml 6.x 에서 호환성 오류 발생 → parser 를 `"html.parser"` 로 일시 전환 + 별도 이슈 분리
- smoke test 는 통과했으나 실 RSS pull 에서 `get_text(strip=True)` 산출물이 5.x 와 다른 경우 → 본문 diff 첨부 후 리드 검토
