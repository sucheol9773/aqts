# ══════════════════════════════════════
# AQTS Backend Dependencies
# 모든 버전은 호환성 검증 후 고정
# ══════════════════════════════════════
 
# ── Web Framework ──
fastapi==0.135.3
uvicorn[standard]==0.30.1
pydantic==2.12.5
pydantic-settings==2.3.4
python-multipart==0.0.26
 
# ── Database ──
sqlalchemy==2.0.31
# SQLAlchemy 2.0 async 엔진은 greenlet 에 의존하지만 transitive dep 이라 환경에 따라 누락되면
# `ValueError: the greenlet library is required` 가 발생하고, `orders.py` 같은 광범위 except 블록에서
# 조용히 `success=False` 로 삼켜지는 silent miss (CLAUDE.md §8) 를 유발. 명시 고정으로 재발 방지.
greenlet==3.4.0
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9
 
# ── MongoDB ──
motor==3.4.0
pymongo==4.7.3
 
# ── Redis ──
redis==5.0.7
# aioredis==2.0.1
 
# ── Authentication ──
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyotp==2.9.0
 
# ── HTTP Client ──
httpx==0.27.0
aiohttp==3.13.4
websockets==12.0
 
# ── Data Processing ──
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
 
# ── Machine Learning ──
scikit-learn==1.5.1
xgboost==2.0.3
optuna==4.8.0
# torch>=2.6.0  — Dockerfile에서 CPU 전용 빌드로 별도 설치 (CUDA 제외 ~4GB 절감)
# 로컬 설치: pip install torch --index-url https://download.pytorch.org/whl/cpu
# CVE 해소 위해 2.6.0+ 필수 (PYSEC-2024-259, PYSEC-2025-41)
 
# ── Quant / Backtest ──
# vectorbt==0.26.2
ta==0.11.0
# empyrical==0.5.5  # Python 3.12 비호환 (SafeConfigParser 제거됨), 코드에서 미사용
 
# ── AI / LLM ──
anthropic==0.28.1
 
# ── Scheduling ──
apscheduler==3.10.4
 
# ── Notification ──
python-telegram-bot==21.3
 
# ── Web Scraping ──
beautifulsoup4==4.12.3
lxml==5.2.2
feedparser==6.0.11
praw==7.7.1
 
# ── Rate Limiting ──
slowapi==0.1.9

# ── Monitoring ──
prometheus_client==0.21.1

# ── OpenTelemetry (분산 추적) ──
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
opentelemetry-instrumentation-sqlalchemy==0.46b0
opentelemetry-instrumentation-httpx==0.46b0
opentelemetry-instrumentation-redis==0.46b0
opentelemetry-exporter-otlp-proto-grpc==1.25.0

# ── Utilities ──
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.4.1
pytz==2024.1
 
# ── Testing ──
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==5.0.0
pytest-mock==3.14.0
httpx==0.27.0
factory-boy==3.3.0

# ── Reinforcement Learning ──
gymnasium>=0.29.0
stable-baselines3>=2.4.0
shimmy>=2.0.0
